<UserControl x:Class="HerramientasSICAR.Views.ComentadorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <!-- Header -->
            <TextBlock Text="Comentador de Textos" 
                      Style="{StaticResource TitleTextStyle}"/>


            <!-- File Selection Card -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Archivo de Entrada" 
                              Style="{StaticResource SubtitleTextStyle}"/>
                    
                    <TextBlock Text="LISTA DE TEXTOS EXCEL" 
                              Style="{StaticResource LabelTextStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                Text="{Binding RutaEntrada, UpdateSourceTrigger=PropertyChanged}"
                                materialDesign:HintAssist.Hint="Selecciona el archivo Excel (.xlsx)"
                                IsReadOnly="True"
                                Margin="0,0,8,0"/>

                        <Button Grid.Column="1"
                               Command="{Binding SeleccionarArchivoCommand}"
                               Style="{StaticResource MaterialDesignRaisedButton}"
                               materialDesign:ButtonAssist.CornerRadius="4"
                               Height="40">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileOutline" 
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="Seleccionar" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Output Configuration Card -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Archivo de Salida" 
                              Style="{StaticResource SubtitleTextStyle}"/>
                    
                    <TextBlock Text="NOMBRE DEL ARCHIVO MODIFICADO" 
                              Style="{StaticResource LabelTextStyle}"/>
                    
                    <TextBox Text="{Binding NombreSalida, UpdateSourceTrigger=PropertyChanged}"
                            materialDesign:HintAssist.Hint="Ej: TextList_Modified.xlsx"
                            FontSize="14"/>
                </StackPanel>
            </Border>

            <!-- Action Card -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Button Command="{Binding EjecutarCommand}"
                           Style="{StaticResource MaterialDesignRaisedButton}"
                           materialDesign:ButtonAssist.CornerRadius="4"
                           Height="48"
                           FontSize="15"
                           IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}"
                           Background="{StaticResource PrimaryBrush}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlayCircleOutline" 
                                                   Width="24" 
                                                   Height="24"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,12,0"/>
                            <TextBlock Text="Procesar Archivo" 
                                     VerticalAlignment="Center"
                                     FontWeight="Medium"/>
                        </StackPanel>
                    </Button>

                    <!-- Status -->
                    <StackPanel Orientation="Horizontal" 
                               Margin="0,16,0,0"
                               Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                    Value="0"
                                    IsIndeterminate="True"
                                    Width="24"
                                    Height="24"
                                    Margin="0,0,12,0"/>
                        <TextBlock Text="Procesando archivo..." 
                                  VerticalAlignment="Center"
                                  Foreground="{StaticResource PrimaryBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Info Card -->
            <Border Background="#E3F2FD" 
                   CornerRadius="8" 
                   Padding="16"
                   Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="InformationOutline" 
                                           Width="24" 
                                           Height="24"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           VerticalAlignment="Top"
                                           Margin="0,0,12,0"/>
                    <TextBlock TextWrapping="Wrap"
                              Foreground="{StaticResource TextPrimaryBrush}">
                        Recuerda activar "Import source language" al importar los textos de nuevo "
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
